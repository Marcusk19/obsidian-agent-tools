#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
EXECUTABLE="${OBSIDIAN_AGENT_SUMMARIZER:-$PROJECT_DIR/dist/summarizer/index.js}"
exec node "$EXECUTABLE" "$@"
